import MainLayout from '@/layouts/MainLayout'

const NotFound = () => {
  return (
    <div className="w-full min-h-screen bg-black flex flex-col items-center justify-center text-4xl text-white">
      404 page not found
    </div>
  )
}

export default NotFound
export const Layout = MainLayout
